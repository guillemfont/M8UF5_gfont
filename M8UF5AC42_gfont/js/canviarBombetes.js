//Funció per canviar a l'imatge del llum apagat
function canviaImatge(elem) {
    elem.style.backgroundImage = "url(img/llumon.gif)"
}

//Funció per canviar a l'imatge del llum trencat
function trencaImatge(elem) {
    elem.style.backgroundImage = "url(img/llumbreak.gif)"
}

//Funció per tornar a l'imatge del llum encés
function tornaImatge(elem) {
    elem.style.backgroundImage = "url(img/llumoff.gif)"
}